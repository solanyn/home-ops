apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lidarr-tls
spec:
  secretName: lidarr-tls-secret
  dnsNames:
    - api.lidarr.audio
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
