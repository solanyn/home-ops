# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - ./helmrelease.yaml
  - ./ocirepository.yaml
configMapGenerator:
  - name: sftpgo-configmap
    files:
      - ./resources/pre-login-hook.sh
generatorOptions:
  disableNameSuffixHash: true
