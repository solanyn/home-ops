---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kanidm-users
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: kanidm-users
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: kanidm
