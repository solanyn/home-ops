# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bookshelf
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: bookshelf-secret
    template:
      data:
        READARR_API_KEY: "{{ .BOOKSHELF_API_KEY }}"
        READARR_POSTGRES_HOST: &dbHost postgres-rw.storage.svc.cluster.local
        READARR_POSTGRES_PORT: "5432"
        READARR_POSTGRES_USER: &dbUser "{{ .BOOKSHELF_POSTGRES_USER }}"
        READARR_POSTGRES_PASSWORD: &dbPass "{{ .BOOKSHELF_POSTGRES_PASSWORD }}"
        READARR_POSTGRES_MAIN_DB: bookshelf_main
        READARR_POSTGRES_LOG_DB: bookshelf_log
        INIT_POSTGRES_DBNAME: bookshelf_main bookshelf_log bookshelf_cache
        INIT_POSTGRES_HOST: *dbHost
        INIT_POSTGRES_USER: *dbUser
        INIT_POSTGRES_PASS: *dbPass
        INIT_POSTGRES_SUPER_PASS: "{{ .POSTGRES_SUPER_PASS }}"
  dataFrom:
    - extract:
        key: bookshelf
    - extract:
        key: cloudnative-pg
