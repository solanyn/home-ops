apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret.yaml
  - ./cronjob.yaml
configMapGenerator:
  - name: gitlab-sync-scripts
    files:
      - sync.py=./resources/sync.py
generatorOptions:
  disableNameSuffixHash: true
  annotations:
    kustomize.toolkit.fluxcd.io/substitute: disabled
