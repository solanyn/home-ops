---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: rustical
spec:
  targetNamespace: default
  components:
    - ../../../../components/volsync
  interval: 1h
  path: ./kubernetes/apps/default/rustical/app
  postBuild:
    substitute:
      APP: rustical
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  wait: false
