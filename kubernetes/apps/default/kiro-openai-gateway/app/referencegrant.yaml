apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: kiro-openai-gateway
  namespace: default
spec:
  from:
    - group: gateway.envoyproxy.io
      kind: Backend
      namespace: network
  to:
    - group: ""
      kind: Service
      name: kiro-openai-gateway
