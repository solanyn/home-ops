---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: suwayomi
spec:
  oidc:
    clientID: suwayomi
    clientSecret:
      name: suwayomi-oidc
    logoutPath: /logout
    provider:
      issuer: https://id.goyangi.io/
    redirectURL: https://suwayomi.goyangi.io/oauth2/callback
    refreshToken: true
    scopes:
      - openid
      - profile
      - email
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: suwayomi