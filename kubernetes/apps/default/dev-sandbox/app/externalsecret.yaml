---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: dev-sandbox
  namespace: default
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: dev-sandbox
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: dev-sandbox
