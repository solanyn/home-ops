apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: lake
spec:
  targetNamespace: default
  dependsOn:
    - name: garage
      namespace: storage
  interval: 1h
  path: ./kubernetes/apps/default/lake/app
  prune: true
  postBuild:
    substitute:
      APP: lake
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  wait: false
