---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: authentik
spec:
  chartRef:
    kind: OCIRepository
    name: authentik
  interval: 15m
  values:
    global:
      podAnnotations:
        secret.reloader.stakater.com/reload: authentik-secret,postgres-authentik-cert
      deploymentStrategy:
        type: RollingUpdate
      securityContext:
        fsGroup: 1000
      volumes:
        - name: postgres-cert
          secret:
            secretName: postgres-authentik-cert
            defaultMode: 0440
        - name: postgres-ca
          secret:
            secretName: cluster-ca
            defaultMode: 0440
        - name: blueprints-flows
          configMap:
            name: authentik-flows
        - name: blueprints-stages
          configMap:
            name: authentik-stages
        - name: blueprints-system
          configMap:
            name: authentik-system
        - name: blueprints-groups
          configMap:
            name: authentik-groups
        - name: blueprint-oidc
          emptyDir: {}
      volumeMounts:
        - name: postgres-cert
          mountPath: /var/run/secrets/postgresql
          readOnly: true
        - name: postgres-ca
          mountPath: /var/run/secrets/root-ca
          readOnly: true
        - name: blueprints-flows
          mountPath: /blueprints/custom/flows
          readOnly: true
        - name: blueprints-stages
          mountPath: /blueprints/custom/stages
          readOnly: true
        - name: blueprints-system
          mountPath: /blueprints/custom/system
          readOnly: true
        - name: blueprints-groups
          mountPath: /blueprints/custom/groups
          readOnly: true
        - name: blueprint-oidc
          mountPath: /blueprints/custom/oidc
          readOnly: true
    server:
      autoscaling:
        enabled: false
      replicas: 1
      envFrom:
        - secretRef:
            name: authentik-secret
      resources:
        requests:
          cpu: 10m
          memory: 64Mi
        limits:
          memory: 1Gi
      metrics:
        prometheus:
          serviceMonitor:
            enabled: true
      route:
        main:
          enabled: true
          hostnames:
            - id.goyangi.io
            - authentik.goyangi.io
          parentRefs:
            - name: envoy-internal
              namespace: network
              sectionName: https
    worker:
      autoscaling:
        enabled: false
      replicas: 1
      initContainers:
        - name: blueprint-sync
          image: ghcr.io/kiwigrid/k8s-sidecar:1.25.2@sha256:cb4c638ffb1fa1eb49678e0f0423564b39254533f63f4ca6a6c24260472e0c4f
          env:
            - name: LABEL
              value: "authentik/oidc=true"
            - name: FOLDER
              value: "/blueprints/custom/oidc"
            - name: RESOURCE
              value: "secret"
            - name: NAMESPACE
              value: "default"
            - name: METHOD
              value: "SLEEP"
          volumeMounts:
            - name: blueprint-oidc
              mountPath: /blueprints/custom/oidc
      envFrom:
        - secretRef:
            name: authentik-secret
      resources:
        requests:
          cpu: 10m
          memory: 64Mi
        limits:
          memory: 1Gi
