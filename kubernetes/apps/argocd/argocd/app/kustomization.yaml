---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
configMapGenerator:
  - name: argocd-values
    files:
      - values.yaml=https://raw.githubusercontent.com/deployKF/deployKF/refs/heads/main/argocd-plugin/argocd-helm/values.yaml
configurations:
  - ./helm/kustomizeconfig.yaml
