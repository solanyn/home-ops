---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: buildbarn-worker
spec:
  chartRef:
    kind: OCIRepository
    name: buildbarn-worker
  interval: 1h
  install:
    timeout: 15m
    remediation:
      retries: -1
  upgrade:
    timeout: 15m
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    defaultPodOptions:
      enableServiceLinks: false
      nodeSelector:
        node.kubernetes.io/nodepool: "true"
      tolerations:
        - key: nodepool
          operator: Equal
          value: "true"
          effect: NoSchedule
    controllers:
      app:
        replicas: 2
        initContainers:
          runner-installer:
            image:
              repository: ghcr.io/buildbarn/bb-runner-installer
              tag: 20260326T163248Z-e6ab874
          volume-init:
            image:
              repository: busybox
              tag: "1.37"
            command:
              - sh
              - -c
              - mkdir -p /worker/build /worker/cache /worker/tmp
        containers:
          app:
            image:
              repository: ghcr.io/buildbarn/bb-worker
              tag: 20260326T163248Z-e6ab874
            args:
              - /config/worker-ubuntu22-04.jsonnet
            env:
              NODE_NAME:
                valueFrom:
                  fieldRef:
                    fieldPath: spec.nodeName
              POD_NAME:
                valueFrom:
                  fieldRef:
                    fieldPath: metadata.name
            resources:
              requests:
                cpu: "1"
                memory: 1Gi
              limits:
                memory: 2Gi
          runner:
            image:
              repository: ghcr.io/catthehacker/ubuntu
              tag: act-22.04@sha256:4142ebe9f4fc7b2e2b1f60d06a6aa774ddff13690c8797e6bd7450e73a64f581
            command:
              - /bb/bb_runner
            args:
              - /config/runner-ubuntu22-04.jsonnet
            resources:
              requests:
                cpu: "1"
                memory: 1Gi
              limits:
                memory: 2Gi
    persistence:
      config:
        type: configMap
        name: buildbarn-config
        globalMounts:
          - path: /config
      runner-installer:
        type: emptyDir
        advancedMounts:
          app:
            runner-installer:
              - path: /bb
            runner:
              - path: /bb
      worker:
        type: emptyDir
        advancedMounts:
          app:
            volume-init:
              - path: /worker
            app:
              - path: /worker
            runner:
              - path: /worker
