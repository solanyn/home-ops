---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nativelink
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    controllers:
      cas:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/tracemachina/nativelink
              tag: sha256-d6c159f3863ed6adc8f058fa20ad97095497767c182c7e3fbcc777bd9631ab48.sig
            command: ["nativelink", "/root/local-storage-cas.json5"]
            env:
              RUST_LOG: warn
            resources: {}
      scheduler:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/tracemachina/nativelink
              tag: sha256-d6c159f3863ed6adc8f058fa20ad97095497767c182c7e3fbcc777bd9631ab48.sig
            command: ["nativelink", "/root/.cache/scheduler.json5"]
            env:
              CAS_ENDPOINT: nativelink-cas.bazel.svc.cluster.local
            resources: {}
      executor:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/tracemachina/nativelink
              tag: sha256-d6c159f3863ed6adc8f058fa20ad97095497767c182c7e3fbcc777bd9631ab48.sig
            command: ["nativelink", "/root/.cache/worker.json5"]
            env:
              CAS_ENDPOINT: nativelink-cas.bazel.svc.cluster.local
              SCHEDULER_ENDPOINT: nativelink-scheduler.bazel.svc.cluster.local
            resources: {}
    service:
      scheduler:
        controller: scheduler
        ports:
          http:
            port: 50052
    route:
      scheduler:
        kind: GRPCRoute
        hostnames: ["{{ .Release.Name }}.goyangi.io"]
        parentRefs:
          - name: internal
            namespace: kube-system
            sectionName: https
