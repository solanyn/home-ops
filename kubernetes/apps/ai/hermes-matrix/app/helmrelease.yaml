---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hermes-matrix
spec:
  chartRef:
    kind: OCIRepository
    name: hermes-matrix
  interval: 1h
  values:
    defaultPodOptions:
      securityContext:
        fsGroup: 65534
        fsGroupChangePolicy: OnRootMismatch
        runAsGroup: 65534
        runAsNonRoot: true
        runAsUser: 65534
        seccompProfile:
          type: RuntimeDefault
    controllers:
      hermes-matrix:
        replicas: 1
        strategy: Recreate
        type: deployment
        containers:
          app:
            image:
              repository: ghcr.io/solanyn/hermes-matrix
              tag: rolling@sha256:db6b44117fe1b9b91b0ad0ff82233205c4de8846147a52a66d7bc9066605ec87
            env:
              HERMES_HOME: /data
              MATRIX_ENCRYPTION: "true"
              MATRIX_SESSION_SCOPE: room
              MATRIX_AUTO_THREAD: "true"
              GATEWAY_PROXY_URL: http://hermes-app.ai.svc.cluster.local:8642
            envFrom:
              - secretRef:
                  name: hermes-matrix
            resources:
              requests:
                cpu: 25m
                memory: 256Mi
              limits:
                memory: 1Gi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
    persistence:
      data:
        existingClaim: hermes-matrix
        globalMounts:
          - path: /data
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
