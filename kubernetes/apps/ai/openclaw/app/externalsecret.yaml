---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openclaw
  namespace: ai
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: openclaw
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: openclaw
