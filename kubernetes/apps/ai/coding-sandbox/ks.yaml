---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: coding-sandbox
spec:
  interval: 1h
  path: ./kubernetes/apps/ai/coding-sandbox/app
  postBuild:
    substitute:
      APP: coding-sandbox
      VOLSYNC_CAPACITY: 50Gi
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: ai
  wait: false
  dependsOn:
    - name: agent-sandbox-core
      namespace: agent-sandbox-system
  components:
    - ../../../../components/volsync
