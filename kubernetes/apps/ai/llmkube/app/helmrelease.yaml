---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
    name: llmkube
spec:
    chart:
        spec:
            chart: llmkube
            version: 0.9.6
            sourceRef:
                kind: HelmRepository
                name: llmkube
    interval: 10m
    values:
        modelSource:
            allowedHostPathRoots:
                - /Users/andrew/.omlx/models
