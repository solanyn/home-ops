---
apiVersion: extensions.agents.x-k8s.io/v1alpha1
kind: SandboxWarmPool
metadata:
  name: opencode-homeops-pool
spec:
  replicas: 1
  sandboxTemplateRef:
    name: opencode-homeops
---
apiVersion: extensions.agents.x-k8s.io/v1alpha1
kind: SandboxWarmPool
metadata:
  name: opencode-mono-pool
spec:
  replicas: 1
  sandboxTemplateRef:
    name: opencode-mono
