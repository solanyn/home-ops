---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai

components:
  - ../../components/alerts

resources:
    - ./namespace.yaml
    - ./hermes/ks.yaml
    - ./honcho/ks.yaml
    - ./kelos/base/ks.yaml
    - ./kelos/ks.yaml
    - ./litellm-operator/ks.yaml
    - ./litellm/ks.yaml
    - ./llmkube/ks.yaml
# presidio removed - presidio-masking caused false positives and latency issues
