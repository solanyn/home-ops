---
apiVersion: networking.liqo.io/v1beta1
kind: Configuration
metadata:
  name: worker
  labels:
    liqo.io/remote-cluster-id: worker
spec:
  remote:
    cidr:
      pod: ${POD_CIDR}
      external: ${EXTERNAL_CIDR}
