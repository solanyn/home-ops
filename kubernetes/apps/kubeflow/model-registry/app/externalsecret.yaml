---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: model-registry-db
  namespace: kubeflow
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: model-registry-db
    template:
      data:
        # Postgres connection string
        # format: postgres://user:password@host:port/dbname?sslmode=disable
        # But wait, the Deployment args use --embedmd-database-dsn with mysql format usually.
        # Check upstream docs or args for postgres.
        # Assuming it uses gorm or similar.
        INIT_POSTGRES_DBNAME: model_registry
        INIT_POSTGRES_HOST: postgres-rw.storage.svc.cluster.local
        INIT_POSTGRES_PASS: "{{ .KUBEFLOW_POSTGRES_PASSWORD }}"
        INIT_POSTGRES_SUPER_PASS: "{{ .POSTGRES_SUPER_PASS }}"
        INIT_POSTGRES_SUPER_USER: postgres
        INIT_POSTGRES_USER: "{{ .KUBEFLOW_POSTGRES_USER }}"
        POSTGRES_Db: model_registry
        POSTGRES_Host: postgres-rw.storage.svc.cluster.local
        POSTGRES_Password: "{{ .KUBEFLOW_POSTGRES_PASSWORD }}"
        POSTGRES_Port: "5432"
        POSTGRES_User: "{{ .KUBEFLOW_POSTGRES_USER }}"
  dataFrom:
    - extract:
        key: kubeflow
    - extract:
        key: cloudnative-pg
