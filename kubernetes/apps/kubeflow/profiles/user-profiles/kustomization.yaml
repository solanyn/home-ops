# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./profile.yaml
  - ./externalsecret.yaml
configMapGenerator:
  - name: kfp-launcher
    namespace: kubeflow-andrew-goyangi-cloud
    literals:
      - defaultPipelineRoot=""
    files:
      - providers=./resources/providers.yaml
