apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./rbac.yaml
  - ./job.yaml
configMapGenerator:
  - name: liqo-scripts
    files:
      - download-tools.sh=./resources/download-tools.sh
      - setup.sh=./resources/setup.sh
generatorOptions:
  disableNameSuffixHash: true
