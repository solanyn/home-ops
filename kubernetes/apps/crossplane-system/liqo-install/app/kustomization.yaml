apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./rbac.yaml
  - ./job.yaml
configMapGenerator:
  - name: liqo-scripts
    files:
      - resources/setup.sh
      - resources/peer.sh
generatorOptions:
  disableNameSuffixHash: true
