---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: crossplane
  namespace: crossplane-system
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: crossplane
  values:
    args:
      - "--enable-usages"
    metrics:
      enabled: true