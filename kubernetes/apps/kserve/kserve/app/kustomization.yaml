apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kserve
resources:
  - https://github.com/kserve/kserve/releases/download/v0.12.1/kserve.yaml
  - https://github.com/kserve/kserve/releases/download/v0.12.1/kserve-runtimes.yaml
  - cert-manager.yaml # Ensure Cert-Manager Certificate is created if needed, though KServe usually manages its own webhook certs via a CA bundle injection or self-signed.
