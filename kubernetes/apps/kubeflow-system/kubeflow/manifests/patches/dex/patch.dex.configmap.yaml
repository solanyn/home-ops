apiVersion: v1
kind: ConfigMap
metadata:
  name: dex
  namespace: auth
data:
  config.yaml: |
    issuer: https://id.goyangi.cloud
    storage:
      type: kubernetes
      config:
        inCluster: true
    web:
      http: 0.0.0.0:5556
    logger:
      level: "debug"
      format: text
    oauth2:
      skipApprovalScreen: true
    enablePasswordDB: false
    connectors:
    - type: oidc
      id: pocket-id
      name: Pocket ID
      config:
        issuer: https://id.goyangi.cloud
        clientID: $dex-oidc-client.OIDC_CLIENT_ID
        clientSecret: $dex-oidc-client.OIDC_CLIENT_SECRET
        redirectURI: https://kubeflow.goyangi.cloud/dex
        insecure: false
        insecureSkipEmailVerified: true
        userNameKey: email
        scopes:
          - openid
          - profile
          - email
