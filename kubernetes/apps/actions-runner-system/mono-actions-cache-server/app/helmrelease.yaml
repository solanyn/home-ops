---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mono-actions-cache-server
spec:
  chartRef:
    kind: OCIRepository
    name: mono-actions-cache-server
  interval: 1h
  values:
    defaultPodOptions:
      enableServiceLinks: false
      nodeSelector:
        node.kubernetes.io/nodepool: "true"
      tolerations:
        - key: nodepool
          operator: Equal
          value: "true"
          effect: NoSchedule
    controllers:
      app:
        replicas: 0
        containers:
          app:
            image:
              repository: ghcr.io/falcondev-oss/github-actions-cache-server
              tag: 9.6.2@sha256:a473eded701722b816fd17899570f3055a4393a181507021961e61cf4e739a60
            env:
              API_BASE_URL: http://mono-actions-cache-server.actions-runner-system.svc.cluster.local:3000
              STORAGE_DRIVER: filesystem
              STORAGE_FILESYSTEM_PATH: /data/cache
              DB_DRIVER: sqlite
              DB_SQLITE_PATH: /data/cache-server.db
              CLEANUP_OLDER_THAN_DAYS: "7"
            resources:
              requests:
                cpu: 50m
                memory: 128Mi
              limits:
                memory: 512Mi
    service:
      app:
        ports:
          http:
            port: 3000
    persistence:
      data:
        type: emptyDir
        sizeLimit: 10Gi
        globalMounts:
          - path: /data
