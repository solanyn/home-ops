---
apiVersion: batch/v1
kind: Job
metadata:
  name: init-prefect-db
spec:
  template:
    spec:
      ttlSecondsAfterFinished: 60
      containers:
        - name: init-db
          image: ghcr.io/solanyn/postgres-init:16
          envFrom:
            - secretRef:
                name: prefect-secret

