---
apiVersion: v1
kind: ConfigMap
metadata:
  name: headplane-config
data:
  config.yaml: |
    server:
      host: "0.0.0.0"
      port: 3000
      base_url: "https://headplane.goyangi.io"
      cookie_secret: "a6a31b710631fb02a4e831fb09ed115a"
      cookie_secure: true
      cookie_max_age: 86400
      data_path: "/var/lib/headplane"
    headscale:
      url: "http://headscale-app.network.svc.cluster.local:8080"
      public_url: "https://headscale.goyangi.io"
      config_path: "/etc/headscale/config.yaml"
    oidc:
      issuer: "https://id.goyangi.io"
      client_id: "headplane"
    auth:
      provider: "oidc"
