---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: BackendTrafficPolicy
metadata:
  name: work-opencode-sse
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: work-opencode
  timeout:
    http:
      requestTimeout: "0s"
      idleTimeout: "3600s"
