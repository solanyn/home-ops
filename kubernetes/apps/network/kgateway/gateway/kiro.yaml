---
apiVersion: gateway.kgateway.dev/v1alpha1
kind: Backend
metadata:
  name: kiro-gateway
  namespace: network
spec:
  type: AI
  ai:
    llm:
      openai:
        authToken:
          kind: Passthrough
      host: kiro-gateway.default.svc.cluster.local
      port: 80

