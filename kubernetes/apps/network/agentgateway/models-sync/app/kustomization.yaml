# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: network
resources:
    - ./ocirepository.yaml
    - ./helmrelease.yaml
configMapGenerator:
    - name: sync-config
      files:
          - sync.sh=./resources/sync.sh
generatorOptions:
    disableNameSuffixHash: true