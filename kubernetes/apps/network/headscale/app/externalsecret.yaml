---
apiVersion: v1
kind: ConfigMap
metadata:
  name: headscale-config
data:
  HEADSCALE_OIDC_ISSUER: https://id.goyangi.io
  HEADSCALE_OIDC_CLIENT_ID: headscale
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: headscale-config-file
data:
  config.yaml: |
    # Minimal config - most settings via env vars
    ---
