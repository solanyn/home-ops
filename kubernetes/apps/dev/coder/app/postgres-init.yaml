---
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-sql
  namespace: coder
data:
  init.sql: |
    CREATE DATABASE coder OWNER postgres;
    GRANT ALL PRIVILEGES ON DATABASE coder TO postgres;
