# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ferretdb
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: ferretdb-secret
    creationPolicy: Owner
  target:
    name: ferretdb-secret
    template:
      data:
        FERRETDB_POSTGRESQL_URL: "postgresql://{{ .FERRETDB_USER }}:{{ .FERRETDB_PASS }}@ferretdb-pg-rw.storage.svc.cluster.local:5432/ferretdb"
  dataFrom:
    - extract:
        key: ferretdb
