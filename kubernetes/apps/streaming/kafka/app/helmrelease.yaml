---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kafka
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: kafka
  values:
    generateNetworkPolicy: false
    watchAnyNamespace: true
