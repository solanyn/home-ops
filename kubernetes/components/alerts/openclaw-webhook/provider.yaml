---
apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: openclaw-webhook
spec:
  type: generic-hmac
  address: http://openclaw-webhook.ai.svc.cluster.local/hooks/flux
  secretRef:
    name: openclaw-webhook-flux-secret
