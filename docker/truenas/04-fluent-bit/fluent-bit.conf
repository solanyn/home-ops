[SERVICE]
    flush        5
    log_level    info
    parsers_file parsers.conf
    storage.path /fluent-bit/positions

[INPUT]
    name              tail
    tag               external.truenas
    path              /var/lib/docker/containers/*/*.log
    parser            docker
    db                /fluent-bit/positions/tail.db
    mem_buf_limit     5MB
    skip_long_lines   on
    refresh_interval  10

[FILTER]
    name              modify
    match             external.truenas
    add               host truenas
    add               source docker

[OUTPUT]
    name              forward
    match             external.truenas
    host              192.168.69.135
    port              24224
