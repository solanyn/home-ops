apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./values.sops.yaml
  - ./helmrelease.yaml
secretGenerator:
  - name: values
    namespace: tailscale
    files:
      - values.yaml=values.sops.yaml
